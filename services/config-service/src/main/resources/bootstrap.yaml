server:
  port: 9090
spring:
  cloud:
    config:
      server:
        native:
          searchLocations: "[classpath:/, classpath:/configs, classpath:/configs/{application}/]"
  profiles:
    active: native
