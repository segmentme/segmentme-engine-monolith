
dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation project(':service')
    testImplementation project(path: ':service', configuration: 'tests')
}
